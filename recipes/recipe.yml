name: fivek
description: custom made linux distro for me.

base-image: ghcr.io/blue-build/base-images/fedora-silverblue-nvidia-open
image-version: latest

modules:

  - type: os-release
    properties:
      NAME: 5K
      PRETTY_NAME: 5K 
      DEFAULT-HOSTNAME: 5k

  - type: script
    scripts:
      - remove-default-bg.sh
